import { useDocumentTitle } from '../hooks/useDocumentTitle';

export function WishlistPage() {
  useDocumentTitle('Wishlist');

  return (
    <div>
      <h1>Wishlist</h1>
      {/* Add wishlist items content here */}
    </div>
  );
}
